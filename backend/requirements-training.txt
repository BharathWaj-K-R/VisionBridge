# Training / dataset-extraction dependencies ONLY.
# Do NOT install this file on the Render production service.
#
# Colab / Python 3.12 known-good combination for the MediaPipe Holistic
# video extraction pipeline used by VisionBridge.
#
# Install in a fresh training runtime with:
#   pip install --no-cache-dir -r backend/requirements-training.txt
# Then restart the Colab runtime before importing MediaPipe.
#
# TensorFlow is intentionally NOT listed. VisionBridge's extraction script
# uses legacy mp.solutions.holistic and stubs MediaPipe's unrelated TensorFlow
# docs import when necessary.

numpy==1.26.4
protobuf==4.25.9
mediapipe==0.10.21
opencv-python-headless==4.10.0.84
pandas==2.2.3
kagglehub
pose-format==0.4.0
