# Dependencies used only for offline ISL landmark extraction and base-model training.
# The live Render backend does not install this file.
#
# MediaPipe 0.10.35 uses the supported Python Tasks API. The old
# mp.solutions.hands API is intentionally not used by the active trainer.
mediapipe==0.10.35
